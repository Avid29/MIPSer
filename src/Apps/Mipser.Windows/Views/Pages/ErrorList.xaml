<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Mipser.Windows.Views.Pages.ErrorList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctc="using:CommunityToolkit.WinUI.Controls"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:behaviors="using:CommunityToolkit.WinUI.Behaviors"
    xmlns:logger="using:MIPS.Assembler.Logging"
    xmlns:logenum="using:MIPS.Assembler.Logging.Enum"
    mc:Ignorable="d">
    
    <UserControl.Resources>
        <Style TargetType="ctc:DataColumn">
            <Setter Property="CanResize" Value="True"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Normal"/>
        </Style>
    </UserControl.Resources>

    <ListView ItemsSource="{x:Bind ViewModel.Messages, Mode=OneWay}" SelectionMode="None" IsItemClickEnabled="True">
        <ListView.Header>
            <Border Padding="8,4,8,4"
                    CornerRadius="4">
                <!--<interactivity:Interaction.Behaviors>
                    <behaviors:StickyHeaderBehavior/>
                </interactivity:Interaction.Behaviors>-->

                <ctc:DataTable>

                    <!-- Severity Icon -->
                    <ctc:DataColumn CanResize="False" MinWidth="24" DesiredWidth="24" MaxWidth="24"/>

                    <ctc:DataColumn x:Uid="MessageDC" Content="Message" DesiredWidth="*" MinWidth="200"/>
                    <ctc:DataColumn x:Uid="FileDC" Content="File" DesiredWidth="180" MinWidth="80"/>
                    <ctc:DataColumn x:Uid="LineDC" Content="Line" DesiredWidth="80" MinWidth="32"/>
                </ctc:DataTable>
            </Border>
        </ListView.Header>
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="logger:Log">
                <ctc:DataRow HorizontalAlignment="Left">
                    <ctc:SwitchPresenter TargetType="logenum:Severity" Value="{x:Bind Severity, Mode=OneWay}">
                        <ctc:Case Value="Error">
                            <ImageIcon Height="12" Source="/Assets/Icons/Severity/Error.svg"/>
                        </ctc:Case>
                        <ctc:Case Value="Warning">
                            <ImageIcon Height="12" Source="/Assets/Icons/Severity/Warning.svg"/>
                        </ctc:Case>
                        <ctc:Case Value="Message">
                            <ImageIcon Height="12" Source="/Assets/Icons/Severity/Message.svg"/>
                        </ctc:Case>
                    </ctc:SwitchPresenter>
                    <TextBlock Text="{x:Bind Message, Mode=OneWay}" Margin="8,0" FontSize="12"/>
                    <TextBlock Text="{x:Bind File, Mode=OneWay}" Margin="8,0" FontSize="12"/>
                    <TextBlock Text="{x:Bind Line, Mode=OneWay}" Margin="8,0" FontSize="12"/>
                </ctc:DataRow>
            </DataTemplate>
        </ListView.ItemTemplate>
        <ListView.ItemContainerStyle>
            <Style BasedOn="{StaticResource DefaultListViewItemStyle}"
                   TargetType="ListViewItem">
                <Setter Property="Padding" Value="8,0" />
                <Setter Property="MinHeight" Value="28" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
            </Style>
        </ListView.ItemContainerStyle>
    </ListView>
</UserControl>
